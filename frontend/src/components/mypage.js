import React from 'react';
import './mypage.css';

const Template = ({form, children}) => {
  return (
    <main className="template">
      <div className="title">
        백앤드 내용
      </div>
    </main>
  );
};

export default Template;